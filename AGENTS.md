# AGENTS.md

本文件定義 AI／自動化代理在本 **SvelteKit 專案**中的預設工作規範。

若專案內另有更細緻或更近層級的規範（例如 `README`、`CONTRIBUTING.md`、`DESIGN.md`、`docs/` 內文件、或子目錄 `AGENTS.md`），請依本文件的「規則優先順序」處理衝突。

## 1. 適用範圍與目標

本文件適用於所有由 AI／自動化代理執行的分析、修改、重構、修復、測試與交付行為。

目標如下：

- 降低代理在實作過程中的判斷歧義。
- 優先遵循 SvelteKit 與專案既有慣例。
- 維持型別安全、可維護性、可測試性與安全性。
- 避免無關修改、過度設計與破壞性調整。

## 2. 回覆與文案語言

- 除非使用者明確要求英文，**所有回覆必須使用正體中文**。
- UI microcopy（介面文案）預設使用**正體中文**。
- 錯誤訊息、提示文字、表單說明與空狀態文案，若無既有慣例，皆使用正體中文。
- 程式碼中的識別字命名、技術術語、第三方套件名稱與 API 名稱可維持英文。

## 3. 技術基線

除非使用者明確要求或專案既有實作另有規定，預設技術基線如下：

- 套件管理：`pnpm`
- 框架：`SvelteKit`
- 建置工具：`Vite`
- 程式語言：`TypeScript`
- 樣式系統：`Tailwind CSS`
- UI 元件：`Shadcn Svelte`
- 程式品質：`ESLint`
- 表單處理：`Superforms`
- 驗證：`Zod`
- 測試：`Vitest`

### 3.1 基線套件不存在時的處理方式

若專案**尚未安裝、導入或實際使用**上述工具，代理不得僅因本文件而任意大規模引入新基礎設施。

處理原則如下：

- 若任務明確要求導入，則可在任務範圍內引入。
- 若專案已有相近方案，優先沿用既有方案，不強制切換。
- 若引入新工具會造成跨模組或跨流程變更，應避免直接擴張範圍，並在交付說明中註記建議方案與原因。
- 若無法確定是否應導入新工具，先參考專案既有實作；仍無法判定時，再詢問使用者。

## 4. 規則優先順序

規則衝突時，依下列順序決定：

### 4.1 一般技術與實作規則

1. 使用者當前指示
2. 更近層級的 `AGENTS.md`
3. 根目錄 `AGENTS.md`
4. 其他專案文件與既有實作慣例

### 4.2 設計與 UI 規則

1. 使用者當前指示
2. 更近層級的 `AGENTS.md`
3. `DESIGN.md`
4. 根目錄 `AGENTS.md`
5. 其他專案文件與既有實作慣例

若設計相關規則與一般實作規則衝突，**設計決策以 `DESIGN.md` 為優先**，但仍不得違反安全、型別與資料邊界規範。

## 5. 任務開始前的標準流程

代理開始實作前，應依序執行以下檢查：

1. 閱讀根目錄 `AGENTS.md`。
2. 檢查目標目錄或相關子目錄是否存在更近層級的 `AGENTS.md`。
3. 檢查根目錄是否存在 `DESIGN.md`；若任務涉及 UI、版面、互動、文案或主題樣式，必須先閱讀。
4. 參考與目標功能最接近的既有實作，優先沿用其模式。
5. 先判斷需求屬於 server、client、共用模組、路由層或純 UI 層，再決定放置位置。
6. 僅修改完成任務所需的最小範圍檔案。

若資訊不足以做出低風險判斷，應先停止猜測，優先參考既有實作；仍無法判定時再詢問使用者。

## 6. 專案結構指引

以下為預設責任分工；若專案已有不同結構，優先遵循既有結構。

- `src/routes/`：SvelteKit 路由入口，包含 `+page.svelte`、`+layout.svelte`、`+page.ts`、`+page.server.ts`、`+layout.ts`、`+layout.server.ts`、`+server.ts` 等。
- `src/lib/components/`：可重用 UI 元件。
- `src/lib/components/ui/`：Shadcn Svelte 元件集合。
- `src/lib/server/`：僅限 server 端使用的邏輯，如資料讀取、整合、轉換、後端服務包裝。
- `src/lib/client/`：僅限 client 端使用的邏輯；僅在 client-only 邏輯明顯增加時再新增。
- `src/lib/states/` 與 `src/lib/*.svelte.ts`：狀態管理與互動邏輯。
- `src/lib/constants/`：跨模組常數與可共享設定值。
- `src/lib/utils/` 與 `src/lib/utils.ts`：低耦合、可重用、易測試的工具函式。
- `src/lib/schemas/`：Zod schemas 與表單／輸入驗證定義。
- `src/lib/types/` 或 `src/lib/types.ts`：共享型別。
- `src/content/`、`src/data/` 或其他內容來源目錄：內容型資料與靜態資料來源（依專案需求命名）。
- `src/test/` 或 `tests/`：測試程式碼。
- `static/`：公開靜態資源。
- `scripts/`：自動化、建置與維護腳本（可依專案規模增減）。

### 6.1 結構演進原則

- 可漸進補強，不要求一次性重構。
- 不因小功能變更而全面改造資料夾結構。
- 僅在複雜度確實增加時，才拆分 `schemas/`、`types/`、`client/` 等目錄。

## 7. SvelteKit 實作原則

- 嚴格遵循 SvelteKit 官方 routing、`load`、form actions、server endpoint 慣例。
- 先判斷資料應於 server 端還是 client 端取得，再選擇 `load`、`actions` 或 client-side fetch。
- 可在 server 端完成的事，優先不要移到 client 端處理。
- 不要將 server-only 邏輯直接暴露給 client。
- 不要在 client 端直接依賴 `src/lib/server/*`。
- 不要把敏感資料、內部錯誤細節或不必要的後端資訊透過 `load` 傳到 client。
- `+page.server.ts`、`+layout.server.ts`、`actions`、`+server.ts` 的責任需明確區分，避免混用。

### 7.1 資料讀取原則

- 頁面初始化資料優先透過 `load` functions 提供。
- 與表單提交、狀態變更、資料寫入相關的流程，優先使用 SvelteKit actions。
- 僅在互動特性確實需要時，才使用額外 client-side 抓取。

## 8. UI 與樣式規範

- 預設採 **mobile-first** 設計。
- 介面需維持簡潔、乾淨、可讀。
- 優先使用 Tailwind utility classes。
- 僅在 utility 無法清楚表達時，才加入少量自訂 CSS。
- 避免新增大型客製 CSS 檔案，避免破壞 utility-first 結構。
- 優先使用 Shadcn Svelte 官方元件與建議用法。
- 擴展 Shadcn 元件時，優先透過 composition、props、slots 或外層封裝處理，避免直接破壞產生後元件原始碼。
- 色彩、主題、間距與語意樣式優先使用設計 token、CSS variables 或 theme variables，避免硬編碼色碼。

### 8.1 Tailwind class 排序

- 若專案已配置 `prettier-plugin-tailwindcss` 或等效工具，**以自動格式化結果為準**。
- 若未配置，自動遵循專案既有檔案中的排序慣例。
- 不得僅為 class 排序而進行與任務無關的大量重排。

### 8.2 DESIGN.md

若根目錄存在 `DESIGN.md`：

- 實作前必須先閱讀。
- UI 設計、版面、色彩、元件使用、文案語氣與互動行為應優先遵循 `DESIGN.md`。
- 若 `AGENTS.md` 與 `DESIGN.md` 發生設計層面衝突，交付時需註記取捨理由。

## 9. 表單、驗證與資料輸入

- 表單處理優先使用 `Superforms`，但前提是專案已導入或任務明確要求。
- 所有外部輸入都必須驗證與清理，包括但不限於：
    - HTTP request body
    - query params
    - route params
    - form data
    - cookies
    - headers
    - CLI 參數
- 輸入驗證與 schema 優先使用 `Zod`，但前提是專案已導入或任務明確要求。
- 新增或修改資料流時，若專案已有 schema 機制，必須同步更新 schema 與型別，避免漂移。

## 10. 狀態管理與元件拆分

- 共用狀態優先使用 Svelte Stores 或 Runes。
- 避免不必要的全域 store；狀態應盡量就近管理。
- 元件應維持單一、可理解的職責。
- 避免肥大元件、過度耦合與隱性共享狀態。
- 當畫面邏輯、資料轉換、事件流程或視覺區塊明顯膨脹時，應拆分為子元件、hooks-like 模組、stores 或工具函式。

## 11. 程式風格與命名

- 優先遵循專案現有的 ESLint、Prettier、tsconfig 與檔案命名慣例。
- 若專案未明確定義，則遵循以下預設：
    - 使用 TypeScript
    - 使用 ES modules
    - 優先使用 `const`
    - 使用具語意的命名
    - 共用元件檔名採 `PascalCase`
    - SvelteKit 路由檔名遵循官方命名規範
- 不要只因風格偏好而改寫與任務無關的既有程式碼。
- 所有格式化、import 排序與 lint 修正應限制在任務相關範圍。

### 11.1 關於 Airbnb Style Guide

若專案明確採用 Airbnb Style Guide 或相容設定，則在可行範圍內遵循。  
若未明確採用，則**以專案現有 lint / format 設定為準**，不要僅因本文件而額外套用抽象風格要求。

## 12. 型別安全

型別安全為強制要求。

- 公開函式、匯出函式、server handlers、資料轉換函式與共享工具函式，需明確標註參數與回傳型別。
- 共用型別、資料結構與 API 介面應使用 `type` 或 `interface` 明確定義。
- 避免使用 `any`。
- 若無法避免使用寬鬆型別，優先使用 `unknown` 搭配縮小處理。
- 不可讓 schema、資料型別與實際資料流長期失配。
- 若函式屬於局部、簡短且型別可由上下文明確推導的 callback，可依可讀性斟酌是否省略顯式回傳型別，但不得影響公開介面的型別可讀性。

## 13. 安全規範

以下規則不可妥協：

- 不得在程式碼、log、測試輸出、文件或回覆中暴露 API keys、密碼、token、session、憑證或其他敏感資訊。
- 不得信任任何外部輸入；所有輸入皆需驗證與清理。
- 嚴禁使用 `eval()`、`exec()` 或等效動態執行機制。
- 不得將內部實作細節直接暴露在對外錯誤訊息中。
- 若需記錄錯誤，應保留必要除錯脈絡，但不得輸出敏感資訊。

## 14. 可存取性（a11y）最低要求

- 表單欄位需有可辨識標籤，例如 `label`、`aria-label` 或等效關聯。
- 所有互動元件需可鍵盤操作。
- 互動焦點需可見。
- 圖像需提供適當替代文字；純裝飾圖像需標示為裝飾用途。
- 不可僅依賴顏色傳遞關鍵資訊。
- 需維持基本可讀對比與可理解的操作流程。

## 15. 效能與依賴原則

- 優先採用 server-side 載入與渲染。
- 避免不必要的 client bundle 膨脹。
- 非關鍵資源可採 lazy loading 或延後載入。
- 新增第三方套件前，需先評估：
    - 套件體積
    - 維護狀況
    - 是否真的必要
    - 是否已有既有依賴可替代
- 不因小需求引入大型依賴。

## 16. 錯誤處理與觀測性

- Server 錯誤回應應維持一致、可追蹤、可理解。
- 不可吞錯；應以適當方式保留錯誤脈絡。
- 對外訊息應可理解，但不可暴露內部細節。
- 外部服務失敗時，應視情況提供降級、重試或明確失敗訊息。
- 日誌內容不得包含敏感資訊。

## 17. 測試規範

- 測試框架預設使用 `Vitest`，必要時可搭配 `happy-dom` 或其他合適環境；前提是專案已有導入或任務明確要求。
- 測試目錄可使用 `src/test/` 或 `tests/`，但同一專案內需保持一致。
- 測試檔命名使用 `*.test.ts` 或 `*.spec.ts`。
- 路由相關邏輯（如 `load`、`actions`、`+server.ts`）應至少涵蓋：
    - 一條主要成功路徑
    - 一條主要失敗路徑
- 工具函式、狀態管理與關鍵商業邏輯應優先補單元測試。
- 第三方服務、I/O、時間與隨機值應在測試中 mock / stub。
- 新增功能與缺陷修復，應同步新增或更新測試，以避免回歸。

### 17.1 無法補測試時

若當前任務無法合理補測試，交付時必須註記：

- 未補測試的原因
- 可能風險
- 建議補測方向

## 18. 變更邊界

除非使用者明確要求，否則不得：

- 進行大規模重構
- 跨模組重新命名
- 清理與任務無關的檔案
- 任意變更公開 API、資料格式、路由行為或外部契約
- 將局部修正擴張為全面性架構改造

若必須進行破壞性調整，交付說明需包含：

- 影響範圍
- 破壞性變更內容
- 回滾方式
- 是否需要呼叫方同步調整

## 19. 實作決策原則

做決策時，依下列原則排序：

1. 先遵循使用者需求
2. 先遵循 SvelteKit 與專案既有慣例
3. 先確認 server / client 邊界
4. 先確保型別與驗證完整
5. 先維持可維護性與可測試性
6. 最後才考慮額外抽象化與微幅優化

若有多種做法，優先選擇：

- 修改範圍較小
- 與現有模式一致
- 容易驗證
- 容易回滾
- 對既有功能風險較低

## 20. 完成定義（Definition of Done）

任務完成時，至少應符合以下條件：

- 回覆與 UI microcopy 使用正體中文
- 實作符合 SvelteKit 慣例
- 未違反 server / client 邊界
- 表單與外部輸入具備必要驗證
- 型別標註完整，無新增 `any`
- 未暴露敏感資訊
- 無使用 `eval`、`exec` 或等效危險機制
- 元件與模組拆分合理，無明顯肥大元件
- 響應式表現合理
- 至少完成可執行的驗證步驟
- 交付說明完整

## 21. 建議驗證流程

若專案提供對應 scripts，優先依序執行：

```bash
pnpm install
pnpm check
pnpm format
pnpm lint
pnpm test
pnpm build
```

### 21.1 驗證流程補充

- 若專案未提供某些 script，執行可替代的等效檢查。
- 至少應完成 `lint` 與 `build` 或其等效驗證。
- 若因環境、依賴、權限或任務範圍限制而未執行某項檢查，需在交付說明中明確註記原因與風險。

## 22. 交付回報格式

交付時至少包含以下內容：

1. 變更摘要
2. 修改檔案清單
3. 驗證結果
    - `lint`
    - `test`
    - `build`
    - 其他執行過的檢查

4. 未完成項目或已知風險
5. 若有規則取捨，附決策理由

建議格式如下：

```md
## 變更摘要

- ...

## 修改檔案

- `src/...`
- `src/...`

## 驗證結果

- lint: ...
- test: ...
- build: ...

## 風險與備註

- ...

## 規則取捨說明

- ...
```

## 23. 不確定時的處理方式

如果你不確定該怎麼做，請遵循以下順序：

1. 先停止猜測，不要直接擴張實作。
2. 優先參考專案中已存在的相似實作。
3. 檢查是否有更近層級 `AGENTS.md` 或 `DESIGN.md` 可依循。
4. 若仍無法做出低風險判斷，再詢問使用者。

## 24. 代理行為底線

- 不要假設專案一定有某個套件、script 或資料夾。
- 不要為了「看起來比較完整」而擅自擴大任務。
- 不要只因個人偏好改動既有程式風格。
- 不要忽略專案內已存在的實作模式。
- 不要輸出無法驗證的結論；若未執行某項檢查，必須明說。
